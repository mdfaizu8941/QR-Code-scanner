# QR-Code-scanner
QR Code Scanner using OpenCV that detects and decodes QR codes in real-time using a webcam. Built with OpenCV for fast QR detection and supports scanning text/URLs instantly.
 # QR Code Scanner using OpenCV 📷🔍

A real-time **QR Code Scanner** built using **OpenCV** that detects and decodes QR codes using a webcam or camera feed.  
The project can scan QR codes containing text, URLs, or other data and displays the decoded result instantly.

---

## 🚀 Features
- Real-time QR code detection using webcam
- Decodes QR codes instantly
- Supports QR codes containing text, links, numbers, etc.
- Displays output on screen
- Lightweight and fast processing using OpenCV

---

## 🛠️ Tech Stack
- OpenCV
- Python / C++ (mention your language)
- Webcam Camera Feed

---

## 📌 How it Works
This project uses OpenCV’s built-in QR detection module (`QRCodeDetector`) to:
1. Capture video frames from the webcam
2. Detect the QR code in the frame
3. Decode the QR data
4. Display the result in real-time

---

## ⚙️ Installation

### 🔹 For Python Version
Install OpenCV:

```bash
pip install opencv-python

